import Stack from '@mui/material/Stack';

import Logo from '../Logo';

export const Header = () => {
    return (
        <Stack>
            <Logo />
        </Stack>
    );
};

export default Header;
